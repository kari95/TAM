<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
      xmlns:tools="http://schemas.android.com/tools"
      tools:showIn="navigation_view"
  >

  <group android:checkableBehavior="single">
    <item
      android:id="@+id/nav_now"
      android:icon="@drawable/ic_av_timer"
      android:title="@string/meal_now"
      />
    <item
      android:id="@+id/nav_planned"
      android:icon="@drawable/ic_restaurant"
      android:title="@string/meal_planned"
      />
  </group>

  <item android:title="@string/account">
    <menu>
      <item
        android:id="@+id/nav_sign_in"
        android:icon="@drawable/ic_account_circle"
        android:title="@string/sign_in"
        />
    </menu>
  </item>

</menu>
